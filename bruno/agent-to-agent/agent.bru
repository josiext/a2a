meta {
  name: agent
  type: http
  seq: 4
}

post {
  url: http://localhost:8000/agent
  body: json
  auth: inherit
}

body:json {
  {
    "prompt": "Cuando es el proximo concierto de Linking park en Chile"
  }
}
